# Vision dependencies
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.37.0         # Florence-2 requires trust_remote_code support
einops>=0.7.0                # required by Florence-2
timm>=0.9.12                 # required by Florence-2
Pillow>=10.0.0
requests>=2.28.0
numpy>=1.24.0

# Vision server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# MuJoCo (required for scene rendering and depth capture)
mujoco>=3.1.0

# FoundationPose (optional — falls back to depth-based pose estimation)
# Install from https://github.com/NVlabs/FoundationPose
# Requires NVIDIA GPU + CUDA toolkit.
# trimesh>=4.0.0             # mesh I/O for FoundationPose reference models

# Dev / testing
# pytest>=8.0
